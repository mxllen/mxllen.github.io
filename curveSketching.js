let A = 1, B = 5, C = 1;

function PQ() {
	let zeros1 = -B / 2 + Math.sqrt((2 / 2) ^ 2 - C);
	let zeros2 = -B / 2 - Math.sqrt((B / 2) ^ 2 - C);
	console.log(zeros1, zeros2);
}

let alpha = beta * ((-1 / 3 * beta ^ 2) + 3);


function curveCalculator() {
	console.log(alpha);
}

f(x)= -1/3x^3+3x 
      x((-1/3x^2) +3)
	  
	  x1 = 0
	  
	  -1/3x^2+3 | *-3
	  x^-9
	